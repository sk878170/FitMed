{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge",
            "request": "launch",
            "name": "Launch Edge against localhost",  // Fix misleading name
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
